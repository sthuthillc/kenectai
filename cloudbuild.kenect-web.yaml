steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --progress=plain
      - -f
      - Dockerfile.kenect-web
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}

options:
  machineType: E2_HIGHCPU_8
  logging: GCS_ONLY

timeout: 3600s
